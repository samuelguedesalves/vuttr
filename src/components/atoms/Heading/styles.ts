import { styled } from '../../../styles/stitches.config';

const H1 = styled('h1', {
  fontSize: 45,
  fontWeight: 'bold',
  fontFamily: '$heading',
  color: '$dark',
});

export const Styles = {
  H1,
};
